import React from 'react';
import ReactDOM from 'react-dom/client';
import StudyLogPopup from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <StudyLogPopup />
  </React.StrictMode>
);
